# xremap

This is a Wayland alternative for AutoKey. 
```
sudo dnf copr enable blakegardner/xremap
sudo dnf install xremap-wlroots
```
Put [config.yml](config.yml) in `~/.config/xremap`
```
sudo xremap ~/.config/xremap/config.yml
```
